import { Inter, Lusitana, Noto_Sans_Javanese } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
});

export const noto_sans_javanese = Noto_Sans_Javanese({ subsets: ['latin']} );
